# Backend API (must include /api/v1 if your server uses that prefix)
PUBLIC_API_BASE_URL=http://localhost:3000/api/v1

# Rsbuild dev server port (optional; default 5730). CLI --port overrides.
# PUBLIC_DEV_PORT=5730
